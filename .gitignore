# Python
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# R
.Rhistory
.RData
.Rproj.user/

# OS
.DS_Store
Thumbs.db

# Notes:
# - Data IS committed (small, public datasets) so the repo runs out-of-the-box.
# - Generated figures/outputs ARE committed so results are browsable without running the code.

# Superseded working files stay on disk but out of the repo.
# The originals also live in the untouched project folder one level up.
archive/
